# GAFFER — İlerleme ve Kararlar Günlüğü (PROGRESS)

> Bu doküman `ROADMAP.md`'nin yanında **"ne yaptık, neyi neden seçtik"** kaydını tutar.
> **ROADMAP = plan** (sıra + çıkış kriterleri); **PROGRESS = gerçekleşen + alınan kararlar.**
> Amaç: ilerlemeyi ve kararları kaybetmemek. Güncel durumu yansıtır (kronolojik değil).

---

## Güncel durum · 2026-07-23

- **Faz 0** ✅ · **Faz 1** ✅ (★ Gate A geçildi) · **Faz 2** 🟡 (çekirdek bitti; JSON adapter **yazıldı** — Unity doğrulaması bekliyor) · **Faz 3** 🟡 (üreteç + kadro→güç + rol-özel rating + gelişim + sezon-entegrasyonu + kadro-yenilenmesi + kulüp-üreteci + **balans-SO** hazır) · **Faz 4** 🟡 (**trait sistemi + dram motoru + moral çekirdeği hazır** — karar #31; SO authoring yüzeyleri yazıldı, Unity doğrulaması bekliyor)
- **210 dotnet testi yeşil** — `dotnet test tests/Gaffer.Tests.csproj` (aynı testler Unity Test Runner'da da koşar)
- Bir lig sezonu **uçtan uca oynanıyor** (headless + **Season Player** editor demosu); **kaydet/yükle çekirdeği** + **deterministik oyuncu üreteci** (garanti wonderkid dâhil) + **kadro→`TeamStrength` köprüsü** (`BuildEffectiveStrength`) hazır. **Season Player artık her kulübe üretilmiş kadro veriyor**, gücü kadrodan türetiyor, yönettiğin **kadroyu isimli oyuncular + rol-anahtarı statlarla** (değere göre renklenen, ART_STYLE §4.1) gösteriyor maç sonuçlarında **isimli golcüleri** ("Doe 23', Roe 67'") listeliyor ve **taktik** (mentality/tempo/pressing) seçtirip gücün canlı kaydığını gösteriyor, **formasyon + Start/Bench ile ilk 11'i seçtiriyor** (güç yalnız sahadaki 11'den), **Approach** (Counter/Possession) seçtiriyor + şans-profilini gösteriyor, ve maç sonuçlarında **şut sayısını** listeliyor
- **Rating artık rol-özel** (bek pace/crossing'den, stoper marking/heading'ten, kanat pace/dribbling'ten, ST finishing'ten puanlanır — aynı stat sayfası mevkiye göre farklı değerde); **oyuncu gelişimi** çekirdeği hazır (genç cevher potansiyele doğru büyür, veteran rol+oyuncu-özel zirveden sonra düşer — herkes 30'da düşmez) → **keşfet-büyüt-sat** döngüsü çekirdekte kapandı. **`SeasonTransition`** ile gelişim **gerçek lig loop'una girdi**: sezon sonu tüm kadrolar yaşlanıp gelişir, **yaşlılar emekli olup yerine aynı mevkide genç gelir** (`SquadRenewal`; kadro boyu + seviye kalıcı), güç yeniden türetilir → **Season Player'da "Start Next Season" ile sürdürülebilir çok-sezonlu run** (cevherin büyümesi/veteranın düşmesi/gençlerin gelişi/tablonun kayması sezonlar boyu görünür). **Transfer Market'ta "Advance Season"** ile de tek pencerede

---

## Gate durumu

| Kapı | Faz | Durum | Ölçüm |
|---|---|---|---|
| ★ Gate A | 1 sonu | ✅ **GEÇILDI** | gol 2.69/maç · favori %51.6 kazanır · ev %38.2 > deplasman %35.8 · şampiyon dağılımı sağlıklı (regresyon testleriyle kilitli) |
| ★ Gate B | 5 sonu | ⬜ | Hikaye kendiliğinden beliriyor mu? |
| ★ Gate C | 8 | ⬜ | MVP Definition of Done |

---

## Tamamlananlar (katman bazında)

- **Common** — `Result`/`Result<T>`, `IRandom` + `SplitMix64RandomNumberGenerator` (golden-locked, `State` getter save için)
- **Domain** — `Attributes` (gruplu ~29 alan: Teknik/Set-piece/Fiziksel&Hareket/Kalecilik), `RoleKeyAttributes` (rol→anahtar-attribute eşlemesi), `Player` (spesifik `Role` taşır, `Position` ondan türer), `PlayerId`, `Position` (geniş hat), `PlayerRole` (RB/CB/LB/DM/CM/AM/RM/LM/RW/LW/ST/GK) + `PlayerRoles` (rol→hat/kısaltma) (Players); `TeamStrength`, `ClubId`, `Club` (opsiyonel `Squad` taşır), `Squad` (immutable + `Add`/`Remove`/`Contains`) (Clubs); `League` (Leagues)
- **Application/Simulation** — `MatchContext`/`MatchImportance`, `MatchCommand → MatchSimulator → MatchOutcome` (Poisson şans üretimi + kalite çözümü, portlar arkasında), `MatchSimulationSettings` (tuned), `EffectiveStrengthBuilder` (`Squad → TeamStrength`: rol-anahtarı attribute'lardan ağırlıklı hat-rating ortalaması; defans rating'i pozisyona duyarlı — kaleci kalecilik grubundan; boş hat → kadro geneline düşer), `PlayerRatings` (**rol-özel** rating: her `PlayerRole` kendi ağırlıklı formülüyle puanlanır [ağırlıklar 1'e toplanır → uniform sayfa identity]; bek pace/crossing/tackling, stoper marking/heading/strength, kanat pace/dribbling, ST finishing — builder + selector + değerleme + maaş + scout paylaşır), `Formation` (11 **spesifik-rol slotu**; 4-4-2/4-3-3/3-5-2/5-3-2/4-5-1 gerçekten farklı şekiller) + `LineupSelector` (her slotu **tam rolüyle** doldurur, yoksa aynı hat, yoksa herhangi; deterministik) → **güç artık tüm kadrodan değil sahadaki 11'den türüyor**, `Tactics` (Mentality/Pressing → güç; **Tempo/Approach → şans modeli**; Balanced = kimlik) + `ChanceProfile` (`FromTactics`: tempo→hacim, approach→hacim/kalite; **Counter = az ama keskin şans**, Possession = çok ama sönük) — `MatchOutcome`/`MatchResult` artık **şut sayısı** taşır (Counter'ın "az şut, klinik" farkı görünür), `IScorerSelector`/`WeightedScorerSelector` (gol olunca golcü seçimi **iki yoldan**: açık oyun [finishing/pozisyon/pace, forvet baskın] + hava topu [heading/jumping/strength, stopere kornerden gol]; kaleci taban ağırlıktan muaf → ~%0.05, efsane anı ama nadir. Ölçüm: FWD ~%48 / MID ~%36 / DEF ~%15 / GK ~%0.05. `MatchCommand` kadroları taşır, `MatchEvent` golcüyü tutar; kadro yoksa golcü null → strength-only maçlar bozulmaz)
- **Application/Season** — `LeagueSeason` artık **canlı kadro güncelleme** taşır (`UpdateSquad(club, squad)`: kulübü immutable yeniden kurar + pinlenmiş dizilimi düşürür → bir sonraki `AdvanceWeek` ilk-11'i yeniden seçer ve gücü yeni kadrodan türetir; imza/satış **aynı hafta** sahaya yansır; strength-only kulüpte no-op) + `SquadOf(club)` (canlı kadro okuyucu; editör bunu okur); ölçüldü: sezon ortasında maks-yetenek kadro takasla o kulübün puanını belirgin artırır. `SeasonTransition` (`ToNextSeason(league, seed, sezonNo)`: sezon sonu her kulübün kadrosunu **yaşlandır+geliştir** [`PlayerDevelopment`] → **yenile** [`SquadRenewal`] → gücü yeniden türet → sonraki sezon `League`'i; oyuncu başına deterministik tohum, yeni gençlere benzersiz id → run tekrarlanır, biri diğerini etkilemez; kadrosuz kulüp dokunulmadan geçer; **opsiyonel `(DevelopmentSettings, RenewalSettings)` ctor** → run boyu gelişim+yenilenme dengesi config'ten ayarlanabilir [cevher kadansı da renewal settings'ten]), `SquadRenewal` (sezon-arası kadro yenilenmesi: yaşlı oyuncular **emekli** olur [deterministik + inandırıcı — sert yaştan sonra kimse oynamaz, kaleci daha geç; alacakaranlıkta sönen düşük-rating'li, oynamaya devam eden yıldızdan önce gider] ve her biri için **aynı mevkide genç** gelir; **ek olarak her sezon garantili akademi alımı** [`YouthIntakePerSeason`, emeklilikten bağımsız — kimse emekli olmasa da genç gelir; **en ince role** yerleşir → hat dengesi]; kadro `MaxSquadSize`'a [25] kadar **büyür** sonra sabitlenir [artık sabit 20 değil — kullanıcı geri bildirimi #30]; gençler kulübün **mevcut seviye bandından** üretilir → seviye kalıcı [güçlü akademi güçlü kalır, en iyi gençler bugünkü 11'i geçebilir]; **garanti-cevher intake-tohumlaması** — `seedGem` bayrağıyla ilk intake genci gem-context'ten [düşük-görünür yetenek + yüksek-gizli potansiyel] gelir, artık **garantili alıma tohumlanır → boş-slot şartı kalktı** [karar #24 iyileştirmesi, #30 — emeklilik olmasa da cevher gelebilir], `SeasonTransition` kulüp başına **kadans**la [seyrek, garanti, yüzde değil] tetikler; kulüp başına ~10 yılda 1; **denge sabitleri enjekte `RenewalSettings`'ten** — emeklilik yaşları/rating-kolaylığı/cevher kadansı+bandı/youth yaş+intake/max-boy, Default=kalibre → refactor davranışı korudu), `FixtureScheduler` (çift devre, circle method), `LeagueTable`, `LeagueSeason` (hafta ilerlet + sonuç geçmişi + `Restore`; her kadrolu kulübün gücünü **ilk 11 + taktikten** her hafta türetir — `SetStarters`/`SetFormation`/`SetTactics`; **her maç kendi rng'sini fikstür kimliğinden tohumlar** → `AdvanceWeek(sim, ctx, seasonSeed)`, bir kulübün taktiği yalnız kendi maçlarını etkiler), `MatchResult` (skor + dakika-golleri), `BoardTarget` + `SeasonEvaluator` → `SeasonVerdict`
- **Application/Serialization** — `SeasonSaveData` DTO (**v3: tam kadrolar + sezon numarası** — çok-sezon run gelişim/yenilenmeyle birlikte kalıcı; `PlayerSaveData`+`AttributesSaveData` [29 alan flat], kadrosuz kulüp strength-only kalır; **v2 [MatchSeed] geriye uyumlu** — eski save kadrosuz → strength-only restore) + `SeasonSaveMapper` (`Capture`/`Restore`: League↔DTO round-trip, oyuncunun her alanı + 29 attribute + yaş + potansiyel + sezon-no bit-aynı geri gelir; testle kilitli) + `SaveSchema`/`SaveMigrator` + `ISerializer` port. **JSON round-trip'i headless kilitli** (`SaveJsonRoundTripTests`: köprüye Newtonsoft eklendi — Unity'nin kullandığı **birebir aynı kütüphane** — serialize→parse→map, her alan + ulong seed + null'lar + 29 attribute korunur; Unity açmadan doğrulanır)
- **Infrastructure/Persistence** — `NewtonsoftJsonSerializer` (`ISerializer` impl; `Serialize` total, `Deserialize` bozuk/yabancı metni `Result`'la yakalar — adapter sınırında beklenen hata) + `JsonSaveStore` (dosya oku/yaz + `SaveMigrator`; eksik/bozuk dosya `Result`, tek-dosya senkron save) + `Packages/manifest.json`'a `com.unity.nuget.newtonsoft-json`. Season Player'a **Save/Load Run** (tüm run'ı — kadrolar+sezon-no+tablo+seed — diske yazar/okur; gelişim+yenilenme reload'da yaşar). *(Unity tarafı — sen derleyip doğrulayacaksın; JSON round-trip zaten headless yeşil.)*
- **Infrastructure/Configuration** — **3 BalanceSO** (ScriptableObject authoring yüzeyleri; pattern: SO Inspector'da ayarlanır → `ToSettings()` saf settings'e map'ler → çekirdek settings'i tüketir, SO'yu değil; **config-as-override** — asset yoksa `Default`): `SimulationBalanceSO` (→ `MatchSimulationSettings`; şans/gol/ev-avantajı/upset), `DevelopmentBalanceSO` (→ `DevelopmentSettings`; büyüme/zirve/gerileme), `RenewalBalanceSO` (→ `RenewalSettings`; emeklilik yaşları/cevher kadansı+bandı/youth). `CreateAssetMenu` Gaffer/Balance/*. **Season Player = balance workbench** (üç opsiyonel SO alanı: sim Start Season'da, gelişim+renewal Start Next Season'da uygulanır → run canlı yanıt verir) + Transfer Market'ta development SO. **Default asset'ler otomatik** (`Editor/Balance/BalanceAssets`: `Assets/_Project/Settings/Balance` altına Unity API'siyle oluşturur+atar — elle YAML yok; pencereler açılışta yükler; `Gaffer/Balance/Create Default Balance Assets` menüsü de var). *(Unity tarafı — derle+gözle doğrula; saf settings'ler zaten headless testli.)*
- **Application/Transfers** — `Finances`'e **`PayWeeklyWages()`** (maaş bordrosunu haftalık nakitten düşer → maaş tavanı artık pasif CAP değil, her hafta gerçek gider; bütçe/bordro değişmez, nakit eksiye düşebilir; gelir modeli ertelendi) + **`TransferWindow`** (`At`/`IsOpen`: transfer yalnız açık dönemde — sezon başı **Summer** + devre-arası **Winter**, dışında **Closed**; saf, oynanmış-round'un fonksiyonu). `PlayerValuation` (piyasa değeri: güncel yetenek + yaş; **gizli potansiyeli fiyatlamaz** → cevher ucuz kalır, flip'in ödülü budur) + `Scout`/`ScoutReport` (maske: rol-anahtarı attribute'lar + potansiyel **aralıkla**; doğruluk arttıkça daralır, hep gerçeği içerir, ortası gerçeği vermez; deterministik) + `PlayerWage` (haftalık maaş: güncel yetenekten) + `Finances` (iki bütçe: transfer nakiti + haftalık maaş tavanı) + `TransferService`/`TransferResult` (düşük-sürtünme al/sat, **fee = değer** [prim/iskonto yok — CM 01/02 sadık]; imza hem nakit **hem** maaş-tavanına sığmalı; gergin ekonomi maaşlardan gelir, para-basma yok çünkü değer=güncel yetenek → gelişmemiş oyuncu break-even; `Result` ile kontroller)
- **Application/Generation** — `PlayerGenerator` (deterministik, **pozisyona uygun** attribute üretimi: fiziksel herkese in-band, teknik outfield'a in-band/kaleciye zayıf, kalecilik grubu kaleciye in-band/outfield'a ~0) + `PlayerNameGenerator` (kurgusal isim) + `PlayerPoolGenerator` (garanti wonderkid) + `SquadGenerator` (spesifik-rol dağılımı: 2 GK, RB/CB×4/LB, DM/CM×3/AM/RM/LM, RW/LW/ST×3 — hat toplamları 2/6/7/5) + `GenerationContext` + `ClubNameGenerator` (**kombinatoryal kurgusal kulüp/lig ismi**: yer-kökü [prefix+ending] + opsiyonel kulüp-sözcüğü ["Ashfield United"/"Brackenmoor"], lig = bölge+turnuva ["Northern League"]; binlerce farklı isim, `GenerationDistinct` lig içinde tekrarsızlığı garanti eder — çakışınca yeniden çeker, numaralı fallback; kurgusal → gerçek kulüp kopyalanmaz, karar #6) + `LeagueGenerator` (**tam üretilmiş lig**: lig+kulüp isimleri + **rank'e göre tier'li kadro** [üst kulüpler yüksek yetenek bandından, alt düşük] + kadrodan güç; oyuncu id'leri rank ile ötelenir → çakışmaz; editörlerdeki inline tier mantığı buraya taşındı, testli; deterministik)
- **Editor (`Gaffer.Editor`)** — `SeasonHarnessWindow` (dağılım/Gate A workbench) + `SeasonPlayerWindow` (bir kulübü **sezonlar boyu** yönet: tablo + **saha görünümü** [formasyon slotlarında oyuncular, **sürükle-bırak** ile ilk-11 diz] + roster + taktik + isimli golcüler + şut + verdict; **oyuncu yanında OVR**; sezon bitince **"Start Next Season"** ile tüm kadrolar yaşlanır+gelişir+**yenilenir**, **"SUMMER" kartı** kim emekli oldu/kim geldi listeler, sezon sayacı ile çok-sezonlu run; **balance workbench**: sim/gelişim/renewal SO alanları) + `TransferMarketWindow` (nakit + maaş bütçesi + baz kadro + market: scout-maskesiyle gez, doğruluk slider'ıyla aralıklar daralır, **Sign/Sell** ile al-sat, maaş bordrosu/yıllık maliyet görünür; **kadro + market OVR ile güçlüden zayıfa sıralı**, kadro satırlarında **tüm attribute** + kendi OVR'ı, market'ta OVR = current ability [potansiyel maskeli]; **"Advance Season (age + develop)"** ile cevher büyür/değer yükselir, veteran düşer — flip tek pencerede) + **`ManagementWindow`** (**birleşik yönetim penceresi**: Season Player + Transfer Market TEK pencerede — sezonu hafta hafta oyna **ve** transferi birlikte yap; canlı **CASH + maaş-headroom** satırı [her Advance Week `PayWeeklyWages` ile nakit erir, eksiye düşünce kırmızı], imza/satış `UpdateSquad` ile kadroyu/gücü/ilk-11'i **anında** günceller + `SyncLeague` ile lig/save/scorer tutarlı, market yalnız **Summer/Winter** dönem-banner'ında actionable [kapalıyken Sell/Sign gizli], saha/sürükle-11 + taktik + tablo + isimli sonuçlar + SUMMER kartı + çok-sezon "Start Next Season" + Save/Load kaynak pencerelerden taşındı)
- **Composition** — `MatchSmokeTest` (Play → Console) + sahne
- **Application/Progression** — `PlayerDevelopment` (`Develop(player, rng) → player`: bir sezon, **CM 01/02-sadık yaş+potansiyel+gap modeli**; genç + potansiyel-altı → **rol-anahtarı attribute'lar** potansiyele doğru büyür [17'de hızlı, orta-20'lerde yavaşlar, geç 20'lere [~29] kadar yavaş sızıntı — 25+ sert duvar değil; büyüme = kalan gap kesri × sezon-varyansı, gap'le kaplanır → tavanı aşmaz], **rol+oyuncu-özel zirve yaşından** sonra (kanat/forvet ~30, kaleci ~34; id-offset [-2,+2] → aynı roldeki oyuncular aynı yaşta düşmez — herkes 30'da düşmez) **iki-parçalı gerileme** [rolün rating attribute'larının genel aşınması + fiziksel ekstra aşınma] → her rolde OVR görünür düşer, pace-bağımlı kanat/bek pozisyonel stoper/kaleciden çok daha hızlı [forvet 80→62, stoper ~36'ya tutar, kaleci 39'da 79], rol-rating'den kendiliğinden çıkar; **sezon-varyansı** [0.6–1.4] mekanik eğriyi kırar; deterministik, enjekte rng; `HiddenPotential` sabit tavan). **Tüm denge sabitleri artık enjekte `DevelopmentSettings`'ten gelir** (büyüme oranları, zirve yaşları, gerileme, varyans, floor'lar; `Default` = kalibre değerler → mevcut davranış birebir korunur, veri-güdümlü NON-NEGOTIABLE #3); `PlayerDevelopment` **ve** `SeasonTransition` settings'i ctor'dan alır. Keşfet-**büyüt**-sat flip'inin "büyüt" parçası.
- **Domain/Traits + Domain/Drama (Faz 4)** — `TraitId`/`MatchStakes`/`MatchTraitModifier`/`Trait`/`TraitCatalog` (9 kalibre trait; tanım veride, `Default` = config-as-override tabanı) ve `DramaEventId`/`DramaCategory`/`DramaTrigger`/`DramaChoice`/`DramaEffect`/`DramaTraitBias`/`DramaEvent`/`DramaCatalog` (10 kalibre olay; kopya alanları localization key). `Player` artık `Traits` taşır (save v4 slug listesi).
- **Application/Drama (Faz 4)** — `DramaEngine` (haftalık tick: koşul filtresi → trait-bias'lı ağırlık → kıtlık zarfı [bütçe+ara+cooldown+once-per-run] → ağırlık-ölçekli ateşleme → seçim; `Resolve` → moral ledger'a, nakit/satış/trait-devri outcome'a), `MoraleLedger` (+`IPlayerConditionSource` portu ile `EffectiveStrengthBuilder`'a), `DramaSettings`+`DramaWeekContext`+`PendingDrama`+`DramaOutcome`. `EffectiveStrengthBuilder` ctx-trait+aura+moral okur; `PlayerDevelopment` gelişim trait'lerini uygular; `LeagueSeason` ctx+moral taşır. Infrastructure: `TraitSO`/`TraitCatalogSO`/`DramaEventSO`/`DramaCatalogSO`/`DramaBalanceSO`.
- **Tests** — 210 test (`Gaffer.Tests`); `Gaffer.Tests.Unity` runtime determinizm kontrolü
- **Altyapı** — `tests/` dotnet köprüsü (net8.0 / LangVersion 9), `.editorconfig` (§1+§2), `.gitignore`

---

## Alınan kararlar (ve gerekçeleri)

1. **.NET 8 (`net8.0`) + `LangVersion 9.0`** — test köprüsü SDK 8'de koşar (`~/.dotnet`, sudo'suz kuruldu). LangVersion 9 = Unity 6 paritesi: köprü, Unity'nin reddedeceği bir şeyi (file-scoped namespace vb.) de reddeder. net9 gereksiz (STS); net8 LTS.
2. **Branch akışı = lokal `--no-ff` merge; branch'ler BÜYÜK feature/faz için** — her küçük dilim için ayrı branch açma (çok branch birikmişti); dilimler tek feature branch'inde commit olarak birikir, feature bütünlenince merge. **Doküman/küçük değişiklik doğrudan `main`'e.** PR/review şimdilik yok. Merge edilmiş branch'ler silinmez (boş/kullanılmayan hariç).
3. **Araçlar Unity Editor Window** — `SeasonHarness` konsol uygulaması yerine editor window'a taşındı (ART_STYLE). **`tests/` dotnet köprüsü KORUNDU** — o bir `.exe`/araç değil, çekirdeğin Unity'siz doğrulanmasını sağlayan test mekanizması.
4. **`TeamStrength` → Domain** — `Club` ve maç sim aynı değer nesnesini paylaşsın, duplication olmasın.
5. **`Club`/`League` Domain'de, sezon orkestrasyonu `Application/Season`'da** (dokümanın katman haritasına uygun).
6. **Kulüp/lig isimleri: kurgusal-üretilmiş (kombinatoryal), sahte-gerçek DEĞİL** — hukuki risk (tanınabilir sahteler bile marka riski), roguelike "kendi dünyan" kimliği, MVP kapsamı ("gerçek isim → sonraya"). İsimler **veri**, localization key değil. Üreteç Faz 3.
7. **Denge: `MeanChanceQuality` 0.20 → 0.17** → gol ~2.69 (hedef ~2.5–3). `BalanceSO` Faz 3'e ertelendi (şimdilik tuned `Default` + workbench slider'ları yeterli).
8. **Save = snapshot + `schemaVersion` + migration + RNG state** — replay-based değil (drama/transfer gelince her kararı replay-edilebilir tutmak kırılgan). Replay determinizmle zaten mümkün; save mekanizması değil.
9. **`.meta` dosyaları elle GUID'li üretildi** — iskelet Unity açmadan commitlenebilsin, stabil kalsın.
10. **Saf çekirdek disiplini** — `Common`/`Domain`/`Application` `noEngineReferences`; oklar hep içe (`Presentation → Application → Domain`).
11. **Editor araçları ≠ ship edilen UI.** Editor window'lar (`Gaffer.Editor`) geliştiriciye hizmet eder; feature geldikçe evrilir, gereksizleşince silinir (ör. `MatchSmokeTest` sonra atılır, `SeasonHarness` kalıcı dev aracı). **Ship edilen oyuncu-UI'si ayrı bir Faz 7 işi** (`Presentation` + `Composition`, runtime, UI Toolkit) — editor window oyuna giremez.
12. **Oyuncu isimleri = jenerik insan isimleri (kombinatoryal, gerçekçi).** Kulüp isimleri kurgusal (gerçek-kulüp değil) ama oyuncu isimleri sıradan ad+soyad → marka değil, belirli gerçek oyuncuyu kopyalamadıkça sorun yok. Milliyet-flavor'lı havuzlar sonraya.
13. **Garanti wonderkid = havuza gem-context tohumlama.** İlk N oyuncu düşük-görünür/yüksek-gizli/genç context'ten üretilip karıştırılır; keşif **nadir** kalır (doğrulama testiyle sınırlı).
14. **CM 01/02 dersleri dokümanlara işlendi** — keşif garantisi (NON-NEGOTIABLE), düşük-sürtünme transfer + gergin ekonomi, blackbox quirk'leri, "asla eklenmeyecekler" listesi, paylaşılabilir legend card (post-MVP).
15. **Attribute seti 6 → gruplu ~29 (FM-benzeri).** Teknik/Set-piece/Fiziksel&Hareket/Kalecilik. FM'in "mental" ekseni **bilinçle yok** — trait+kişilik (Katman 2) soğurur, çift-sayım olmasın; teknik/fiziksel granülerlik zengin ama karakter ayrı katmanda. **`Attributes` artık `readonly struct` değil**, public settable auto-property'li value-struct (29 arg'lık ctor yerine object-initializer; struct kopya semantiği dışarıdan yine değişmez tutuyor). **Üretim pozisyona uygun** (kaleci ↔ outfield ayrımı; rol-key *değer* değil vurgu). **Rol→anahtar-attribute eşlemesi `RoleKeyAttributes` (Domain)**; şimdilik kod, sonra `RoleSO`. Gösterim kuralı ART_STYLE §4.1 (değer→parlaklık rampası, tek accent).
16. **Taktik iki katmana ayrıldı — çift-sayım yok.** Mentality + pressing **efektif gücü** (ATK/MID/DEF) kaydırır; **tempo + approach şans modelini** (`ChanceProfile`) şekillendirir (tempo→hacim, approach→hacim/kalite). Gerekçe: kontra-atak "az ama öldürücü şans"tır → gücü değil şans **kalitesini/geçişini** değiştirmeli. Yalnız enum eklemek "flavor" olurdu (NON-NEGOTIABLE #7); bu yüzden Counter gerçekten farklı **oynar** (şut ~%18 az, kalite ~%20 yüksek → aynı skor, farklı doku; defansif mentality ile eşlenince düşük-skorlu galibiyet). Görünürlük için `MatchOutcome`/`MatchResult` **şut sayısı** taşır. *(Approach'ın defansif geçiş primi + tempo'nun yorgunluk etkisi sonraya.)*
17. **Maç başına deterministik tohumlama (per-fixture seeding).** Sezon tek bir sıralı rng yerine, her maçın rng'sini `karıştır(sezon_seed, hafta, ev_id, deplasman_id)` (SplitMix64 finalizer) ile tohumluyor. Gerekçe: sıralı rng'de bir kulübün taktiği maçtaki çekiş sayısını değiştirip **tüm sezonu kaydırıyordu** → gol-nötr bir değişiklik sıralamayı 6 basamak zıplatıyordu (kullanıcı "çok saçma" dedi, haklıydı — sinyal değil gürültüydü). Artık bir taktik değişikliği **yalnız o kulübün maçlarını** etkiler (ölçüm: Balanced 16 / Counter 16 / Possession 17). Bonus: save `MatchSeed` (sabit) saklıyor, evolving rng-state değil → restore sonrası devam bit-aynı, daha sağlam (`SaveSchema` v1→v2). Determinizm korunur (tek sezon-seed). İzolasyon testle kilitli.
18. **Transfer ekonomisi = CM 01/02 sadık, para-basma DEĞİL.** Fee = piyasa değeri (prim/iskonto **yok** — flat sell-iskontosu kaldırıldı; CM'de yoktu). Gergin ekonomi **maaşlardan** gelir: **iki bütçe** (transfer nakiti + haftalık maaş tavanı, CM'in hallmark'ı), imza her ikisine de sığmalı. Para-basma olmaz çünkü **değerleme güncel yeteneği fiyatlıyor, gizli potansiyeli değil** → gelişmemiş oyuncu break-even round-trip yapar; flip'in kârı **gelişimden** gelir (GDD §4.4: "CM'de AI'nın yanlış-değerlemesi transferi para-basma yapıyordu; GAFFER bunu istemiyor — satmanın bedeli oyuncuyu kaybetmek + board baskısı"). Pazarlık indirimi bir **meta-perk** (sonraya). *(Maaşların sezon boyu nakti eritmesi + gelir modeli sezon-entegrasyonu sonraya; şimdilik maaş tavanı bir CAP.)*
19. **Granüler mevki eklendi (mekanik, seçenek A).** `PlayerRole` (RB/CB/LB/DM/CM/AM/RM/LM/RW/LW/ST/GK) — `Player`'ın **birincil kaynağı Role**, geniş `Position` (hat) `PlayerRoles.Line(role)` ile türer (tek doğruluk kaynağı; kanat→Forward, geniş-orta→Midfielder ayrımı 4-4-2 ile 4-3-3'ü gücte farklı tutar). `Player`'a legacy Position ctor korundu (Representative rol) → mevcut çağrılar bozulmadı. `Formation` artık **11 rol slotu**; `LineupSelector` her slotu **tam rolüyle** doldurur (yoksa aynı hat, yoksa herhangi). `SquadGenerator` spesifik rol dağıtır. *(Rol-özel rating sonra geldi → karar #20.)*
20. **Rating rol-özel yapıldı (flavor değil, mekanik).** `PlayerRatings` broad-hat `Attack/Midfield/Defence` yerine her `PlayerRole` için ayrı ağırlıklı formül (`ForRole`): bek pace/crossing/tackling'den, stoper marking/heading/strength'ten, kanat pace/dribbling'ten, ST finishing/positioning'ten puanlanır. Ağırlıklar **1'e toplanır** → uniform stat sayfası her rolde aynı değere çöker (Gate A sayıları değişmedi, `EffectiveStrengthBuilder` uniform-kadro invariant'ı korundu). Sonuç: **aynı stat sayfası mevkiye göre farklı değerde** (hızlı-kanatlı oyuncu bek olarak stoperden çok daha yüksek; hava-topu stoperi bekten yüksek), ve mevkisi dışında oynayan oyuncu doğal fit'in altında rate'lenir. `EffectiveStrengthBuilder` her oyuncunun **kendi rol-rating'ini** mans ettiği hattın eksenine katar (attacking bek de, stopper da defans eksenini gerçekten iyi olduğu şeyle kaldırır); `LineupSelector`/`PlayerValuation`/`PlayerWage`/`Scout` hepsi rol-rating + rol-özel anahtar attribute okur. `RoleKeyAttributes` `For(PlayerRole)` overload'u (anahtarlar rating formülünü aynalar). Ağırlıklar sonra `BalanceSO`.
21. **Oyuncu gelişimi eklendi ("büyüt" — flip'in eksik parçası). CM 01/02-sadık: yaş + potansiyel + gap güdümlü, antrenmana bağlı DEĞİL** (antrenman MVP zorunluğu değil — karar). `Application/Progression/PlayerDevelopment.Develop(player, rng)` bir sezon: genç + potansiyel-altı → **rolün rating attribute'ları** potansiyele doğru büyür (17'de hızlı, orta-20'lerde yavaşlar, **geç 20'lere [~29] kadar yavaş bir sızıntı** — CM'de geç-gelişen oyuncu tavanına ulaşana dek azar azar ilerler, **25+ sert bir duvar değil**; büyüme miktarı **kalan gap'in bir kesri × sezon-varyansı [0.6–1.4]** ve gap'le kaplanır → tavana **yaklaşır ama aşmaz** — PA bir tavandır, hedef değil). **Gerileme herkes için 30'da başlamaz (CM-sadık, kullanıcı sorusu).** Başlangıç **rol-bazlı zirve yaşı** + **oyuncuya-özel offset** [-2,+2, id'den deterministik, havuza dengeli yayılır, min 30]: kanat/forvet ~30, bek/geniş-orta ~31, merkez/pozisyonel ~32, **kaleci ~34** (en geç). Zirve-sonrası yıl sayısı hızlanan bir düşüş sürer. Düşüş **iki parçalı**: (1) rolün rating attribute'larının **genel aşınması** (yaşla kalite düşer → her rolde OVR görünür düşer) + (2) üstüne **fiziksel** (pace/accel/agility/stamina/jumping) ekstra aşınma → pace-bağımlı kanat/bek pozisyonel stoper/kaleciden **çok daha hızlı** düşer (ölçüldü: forvet 30'larında 80→62, stoper ~36'ya tutar sonra →75, kaleci 39'da hâlâ 79; aynı roldeki iki oyuncu artık aynı yaşta düşmez). Eski model yalnız fizikseli aşındırıyordu (stoper/kaleci hiç düşmüyordu) **ve** herkesi 30'da düşürüyordu — ikisi de düzeltildi (growth switch'i işaretli `AdjustRoleAttributes` olarak yeniden kullanıldı; gerçek sürücü — profesyonellik/kişilik — id-proxy'sini Faz 4'te değiştirir). **Sezon-varyansı** gelişim/gerilemeyi mekanik bir eğri olmaktan çıkarır (bir sezon patlar, bir sezon durur — CM hissi), rng'den çekilir → deterministik. Girdiyi mutasyona uğratmaz, yeni `Player` döner; `HiddenPotential` sabit. Gerekçe: değer güncel yeteneği fiyatlar (karar #18) → cevheri ucuz al, **büyüt**, yüksekten sat; kâr gelişimden gelir. **Not:** "oynamazsa geriler" = oynama-süresi sistemi (CM'nin gerilemesi de esasen yaş güdümlü, oradan değil); minutes + `DevelopmentModifier` trait (antrenman kaçkını, TDD) sonraki dilim/Faz 4. *(Sezon-sonu entegrasyonu → karar #22; save kalıcılığı sonraki dilim.)* Transfer Market **kadro OVR + tüm attribute + "Advance Season"** ile döngüyü gösterir (market'ta OVR scout-maskesi altında, yalnız Reveal ile).
22. **Gelişim sezon loop'una girdi (`SeasonTransition`).** Gelişim sezonlar-arası olur → tek başına Transfer Market demosunda kalması yeterli değildi; Faz 3 çıkış kriteri ("düşük-görünür genç patlar") gelişimin **asıl lig loop'unda** koşmasını ister. `SeasonTransition.ToNextSeason(league, seed, sezonNo)` sezon sonu **tüm kulüplerin kadrolarını** `PlayerDevelopment` ile yaşlandırıp geliştirir, her kulübün gücünü gelişmiş kadrodan yeniden türetir, sonraki sezon `League`'ini döner. Her oyuncu **kendi rng'siyle** (tohum = karıştır[seed, id, sezonNo]) gelişir → run tekrarlanır, biri diğerini kaydırmaz; kadrosuz (strength-only) kulüp dokunulmadan geçer. Season Player'a **"Start Next Season"** (formasyon/taktik/hedef taşınır, 11 gelişmiş kadrodan yeniden seçilir, sezon sayacı) → çok-sezonlu run: cevher büyür, veteran düşer, tablo kayar. **Ertelendi:** emeklilik + genç-alımı → karar #23; **save'de gelişmiş kadro kalıcılığı** (serialization, Faz 2 JSON ile) → sonraki dilim.
23. **Emeklilik + genç-alımı (`SquadRenewal`).** Sezon-entegrasyonu (#22) kadroları yaşlandırıyordu ama yenilemiyordu → çok-sezonlu run'da kadrolar sonsuza kadar yaşlanırdı. `SquadRenewal` sezon geçişinde: yaşlı oyuncular **emekli** (deterministik, per-player rng; **sert yaş**tan [outfield 40, kaleci 43] sonra kesin, **alacakaranlık**tan [33/36] önce hiç; arada olasılık yaşla artar, rating'le azalır → sönen düşük-rating'li yıldızdan önce gider — CM-benzeri) ve her emekli için **aynı mevkide bir genç** gelir → **kadro boyu + hat dengesi sabit** (bire-bir). Gençler kulübün **mevcut ortalama seviyesi bandından** üretilir (16-18 yaş, ham ama tavanlı) → **seviye kalıcı** (güçlü kulübün akademisi güçlü, en iyi genç bugünkü 11'i geçebilir); yeni oyuncular **benzersiz id** alır (ligdeki maks+1'den). `SeasonTransition` artık geliştir→yenile→güç sırasıyla çağırır. Ölçüm (15 sezon): boy sabit 20, yaş aralığı sınırlı (patlamıyor), avgOVR ~56-59 (seviye korunuyor), gençler akıyor. Season Player'da **"SUMMER" kartı** (kim emekli oldu / kim geldi, id-diff'iyle) → run bir hikâye gibi okunur. **Ertelendi:** kadro-arası AI transferleri.
24. **Garanti-cevher intake-tohumlaması (süregelen keşif fantezisi, CM/FM-sadık — yüzde DEĞİL).** Kullanıcı vurgusu: keşfin kesinliğini ve heyecanını bozacak per-oyuncu yüzdesi olmamalı. Çözüm `PlayerPoolGenerator`'ın garanti-sayı yaklaşımını aynalar: `SquadRenewal.Renew(..., seedGem)` açıkken (ve boş slot varken) intake genci **gem-context**'ten üretilir — **düşük-görünür yetenek** (sıradan gençten ability ile ayırt edilemez, göz önünde saklanır) + **yüksek-gizli potansiyel** (86-96); ucuza gelir, gelişince servet eder. `SeasonTransition` bunu **kulüp başına deterministik kadans**la [`(sezonNo+clubId)%5==0`] tetikler → **seyrek + garanti + yayılmış**, asla per-oyuncu şansı değil. Ölçüm (20 sezon, 20 kulüp): lig geneli ~2.3/sezon (gelecekteki market-keşfini besler), **kulüp başına ~10 yılda 1** (kendi akademinden homegrown cevher özel hissettirir). **Bilerek REVEAL yok**: cevher düşük-OVR bir genç olarak gelir, yalnız **gelişerek yıldıza döner** → keşif mevcut çok-sezon UI'sinde (OVR tırmanışı) kendiliğinden belirir; işaretlemek büyüyü öldürürdü. *(Başlangıç kadrolarının garanti-cevheri Season Player kurulumunda ayrı bir küçük iş; şimdilik intake akışı fantaziyi taşıyor.)*
25. **Çok-sezon save kalıcılığı (kadrolar + sezon-no; schema v3).** Eski save yalnız **strength** saklıyordu → çok-sezonlu run'da gelişim/yenilenme kaybolurdu. `SeasonSaveData` v3'e çıktı: her kulüp **tam kadrosunu** (`PlayerSaveData`: id/isim/milliyet/rol/yaş/gizli-potansiyel + `AttributesSaveData` 29 alan flat) ve run **sezon numarasını** saklar. `SeasonSaveMapper` League↔DTO round-trip'i: oyuncunun her alanı + 29 attribute + yaş + potansiyel bit-aynı geri gelir (testle kilitli, distinct-değerli attribute'larla mis-map yakalanır). **Geriye uyumlu:** kadrosuz kulüp (harness fixture) veya **v2 save** (kadro alanı yok) → strength-only restore, çökme yok; `SaveMigrator` v2→v3'ü sorunsuz taşır (yeni alanlar null/0 default). Attribute'lar **isimli flat POCO** (byte[] değil) → JSON'da okunur + yeniden-sıralamaya dayanıklı. **Ertelendi (Unity tarafı):** gerçek dosya I/O = `Infrastructure/Persistence` JSON `ISerializer` impl + Season Player'a Save/Load düğmeleri (çekirdek DTO/mapper hazır, JSON adapter'ı bekliyor). *(→ karar #26'da yapıldı.)*
26. **JSON save adapter + editör Save/Load (Faz 2 kapanışına doğru).** "Unity tarafı işi ben yapamam" düzeltmesi: **kodu yazabilirim, yalnız burada derleyemem** (Infrastructure/Editor `UnityEngine`'e referans verir; köprü sadece saf çekirdeği derler → editör pencereleri gibi Unity'de doğrulanır). `Infrastructure/Persistence/NewtonsoftJsonSerializer` (`ISerializer` impl; `Deserialize` bozuk metni `Result`'la yakalar) + `JsonSaveStore` (dosya I/O + migrator, `Result`) + `manifest.json`'a Newtonsoft paketi + Editor asmdef'e Infrastructure ref + Season Player Save/Load. **Kilit hamle: JSON'u headless doğruladım** — köprüye **Newtonsoft** (Unity'nin `com.unity.nuget.newtonsoft-json` ile birebir aynı kütüphanesi) `PackageReference` olarak eklendi, `SaveJsonRoundTripTests` DTO grafiğini serialize→parse→map ile kanıtlar → production serializer'ı Unity açmadan yeşil. Unity tarafında kalan tek şey **derle + Save/Load'u gözle doğrula** (paket resolve olur). Async I/O sınırı (TDD §10) save büyüyünce; şimdilik tek-dosya senkron. **Ek düzeltme:** `Gaffer.Tests.asmdef` `overrideReferences: true` (nunit sabit) → auto-referenced Newtonsoft dışlanıyordu (Unity'de CS0246); `precompiledReferences`'a `Newtonsoft.Json.dll` eklendi (Infrastructure `overrideReferences: false` olduğu için zaten görüyor).
27. **Kulüp/lig ismi üretildi (`ClubNameGenerator`+`LeagueGenerator`) — "dünya üretilir" (karar #6).** Kulüp isimleri iki editörde sabit diziydi. `ClubNameGenerator` kombinatoryal kurgusal isim üretir (yer-kökü prefix+ending + opsiyonel kulüp-sözcüğü; lig = bölge+turnuva; binlerce kombinasyon; `GenerateDistinct` lig-içi tekrarsızlık garantisi, çakışınca yeniden-çek + numaralı fallback; deterministik). `LeagueGenerator` tam bir `League` kurar: lig+kulüp isimleri + rank'e göre tier'li kadro (üst=yüksek band, alt=düşük — editördeki `ContextForRank` inline mantığı buraya **taşındı + testlendi**) + kadrodan güç; oyuncu id'leri rank·SquadSize ile ötelenir → kulüpler-arası çakışma yok. **Season Player** (`BuildLeague` → `LeagueGenerator`; sabit `ClubNames` dizisi + inline tier silindi, takım-sayısı sınırı 64) ve **harness `LeagueFactory`** (isimler `GenerateDistinct`'ten) artık üretilmiş isim kullanır. *(Milliyet-flavor'lı isim havuzları + kulüp arma/renk üretimi sonraya.)*
28. **BalanceSO pattern kuruldu — gelişim dengesi veri-güdümlü (NON-NEGOTIABLE #3).** Sabitler koda gömülüydü. **Pattern:** çekirdek **saf injectable settings** tüketir (headless test edilir), Infrastructure **SO** onu **authoring** eder (Unity Inspector) — saf katman SO'yu değil, map'lenmiş settings'i tüketir (CLAUDE.md config→model köprüsü). `PlayerDevelopment`'ın tüm sabitleri (büyüme oranları, rol-zirve yaşları, gerileme, varyans, floor'lar) → `DevelopmentSettings` (mutable, defaultlu; `Default` = kalibre → **refactor davranışı birebir korudu**, mevcut testler regresyon kilidi); `PlayerDevelopment(settings)` + `SeasonTransition(settings)` ctor'ları (parametresiz = Default → mevcut çağrılar bozulmadı). Infrastructure `DevelopmentBalanceSO` (`ToSettings()` map'ler; asset yoksa Default; config-as-override). Transfer Market'a SO alanı → Advance Season ona göre. **Neden bu ilk:** sim dengesi (`MatchSimulationSettings`) zaten injectable'dı; gelişim en çok "tune later" işaretlenen ve en gameplay-kritik alandı. **Tamamlandı (kullanıcı: "iş kalmasın"):** üç alan da veri-güdümlü — `DevelopmentSettings`+`DevelopmentBalanceSO`, `RenewalSettings`+`RenewalBalanceSO` (emeklilik/cevher kadansı+bandı/youth; `SquadRenewal`+`SeasonTransition` tüketir), `MatchSimulationSettings` (zaten injectable)+`SimulationBalanceSO`. **Season Player = balance workbench**: üç opsiyonel SO alanı → tam çok-sezonlu run'ı ayarlı sim/gelişim/renewal ile oynayıp etkiyi gör (editörde uçtan uca test edilebilir). *(Editör SO-ataması Unity tarafı; tüm saf settings'ler headless testli. Youth ability/potansiyel band-offset'leri ve trait/kişilik sürücüleri Faz 4'e yapısal kaldı.)*

29. **Birleşik yönetim penceresi + ekonominin sezon loop'una entegrasyonu (`ManagementWindow`).** Kullanıcı fikri (planlı kayıt, karar #28-sonrası "Kalan"): Transfer Market + Season Player **tek pencerede** birleşsin → bir sezonu oynarken transfer yap + simüle et **birlikte**. Vurgu: bu **sadece UI birleştirme değil**, ekonominin sezon loop'una entegrasyonu. **Üç çekirdek dilim + bir editör dilim** (her biri çekirdek + dotnet testi + görünür editör + commit; hepsi `feature/unified-management-in-season`'da birikti, `--no-ff` merge — karar #2): **(a) maaşlar haftalık nakti eritir** — `Finances.PayWeeklyWages()` (bordroyu nakitten düşer; **maaş tavanı pasif CAP'ten gerçek gider'e döndü**, GDD §4.4 — para-basma yok çünkü değer=güncel yetenek [karar #18], gergin ekonomi artık her hafta nakit erimesiyle **gerçek**; nakit eksiye düşebilir = board'un göreceği overspend; **gelir modeli [gate/prize] bilinçle ertelendi** — bu dilim CAP'i gidere çevirmekle sınırlı). **(b) imza/satış canlı etkiler** — `LeagueSeason.UpdateSquad(club, squad)` (kulübü immutable yeniden kurar + pinlenmiş dizilimi düşürür → bir sonraki hafta ilk-11 yeniden seçilir, güç yeni kadrodan türer → transfer **aynı hafta** sahaya çıkar) + `SquadOf`; testle kilitli (maks-yetenek takas puanı belirgin artırır). **(c) transfer dönemleri** — `TransferWindow.At/IsOpen` (Summer=sezon başı, Winter=devre-arası; dışında Closed; saf/deterministik). **Editör:** `ManagementWindow` iki pencereyi birleştirir — imza `Finances`'i günceller + `UpdateSquad`'ı çağırır + `SyncLeague` ile lig/save/scorer tutarlılığını korur, Advance Week her hafta `PayWeeklyWages`, market yalnız açık dönemde actionable, geri kalan (saha/taktik/tablo/çok-sezon/Save-Load) kaynak pencerelerden taşındı. **Eski iki pencere korundu** (karar #11 — dev araçları evrilir/gereksizleşince atılır; şimdilik ikisi de kalıyor). **Ertelendi:** ekonomi/dizilim/taktik save-kalıcılığı (finances+market reload'da setup'tan yeniden seed), AI kulüpler-arası transferler, gelir modeli. *(Editör pencere kodu `UnityEngine`'e referans → dotnet köprüsünde derlenemez; elle yazıldı + elle GUID'li .meta [karar #9], Unity'de derle+gözle doğrulanacak. Üç saf çekirdek dilimi headless testli: 169 yeşil, +12.)*
30. **Faz-4-öncesi düzeltmeler (kullanıcı geri bildirimi) — akademi alımı + market determinizmi + pozisyon filtresi.** Üç dilim, `feature/academy-intake-and-market-fixes`, `--no-ff` merge. **(A) Garantili akademi alımı [çekirdek]:** eski `SquadRenewal` yalnız emekli olunca genç getiriyordu → emeklilik olmayan sezonda kadro tamamen statikti. Artık **her sezon garantili akademi alımı** (`RenewalSettings.YouthIntakePerSeason`=1, emeklilikten bağımsız) **en ince role** yerleşir; kadro `MaxSquadSize`'a (25) kadar **büyür** sonra sabitlenir (sabit 20 invariant'ı **bilinçle değişti** — kullanıcı "emekli olmasa da yeni oyuncu eklenmeli"). Garanti-cevher artık bu alıma tohumlanır → **boş-slot şartı kalktı** (karar #24'ün gating'i düzeldi: cevher emeklilik olmadan da gelebilir). İki alan `RenewalBalanceSO`'da author'lanır. Gelişim/yaşlanma testleri değişimi `YouthIntakePerSeason=0` ile izole eder; yeni testler büyüme-tavana + alım-sayısı + cap'te-durma. **(B) Market determinizmi [editör]:** `ManagementWindow` market'ı sezon-bağımsız seed'den üretiyordu → her sezon **aynı isim+pozisyon** oyuncular çıkıyordu (kullanıcı yakaladı). Seed **ve** id-aralığı artık sezon numarasıyla ötelenir → her sezon gerçekten taze kısa liste + önceki sezonun imzasıyla id çakışmaz. **(C) Market filtreleri [editör]:** transfer kartına **spesifik-rol** dropdown'u (Sol Bek/Sağ Bek/Stoper/… — broad `Position` değil `PlayerRole`, kullanıcı isteği) **+ min/max yaş** aralığı; eşleşme yoksa "no prospects match this filter" notu. 172 yeşil (+3). *(A headless testli; B+C editör → Unity'de doğrula.)*

31. **Faz 4 çekirdeği: trait sistemi + dram motoru + moral (oyunun ruhu).** Dört dilim, `feature/phase4-character-drama`, `--no-ff` merge. **(a) Trait sistemi (veri-güdümlü, NON-NEGOTIABLE #3+#7):** saf `Domain/Traits` — `TraitId` (string slug; tanım veride), `MatchStakes`/`MatchTraitModifier` (bahis-koşullu rating çarpanı; koşul saf veri, **değerlendirme Application'da** → domain sim tipine uzanmaz), `Trait` (maç çarpanı + takım-aurası + gelişim çarpanı + zirve-kayması + düşüş-hızı — her alan gerçek bir kol, flavor alan yok), `TraitCatalog.Default` (**9 kalibre trait**: derbi-canavarı ×1.12 derbi/rekabette, büyük-maçta-kaybolan ×0.88, showman [25k+ seyirci], soyunma-odası-lideri [aura ×1.03 **takım arkadaşlarına, kendine değil**], antrenman-kaçkını [büyüme ×0.5], profesyonel [×1.25 + zirve +2], cam-adam [zirve −3 + aşınma ×1.5], sadık + basın-mıknatısı [mekanikleri = referans veren olay ağırlıkları]). `Player.Traits` (opsiyonel ctor param → hiçbir çağrı bozulmadı; save v4 slug listesi, v3 geriye-uyumlu, **bilinmeyen slug taşınır atılmaz** — katalog id'yi kullanımda çözer). Üretimde **ağırlıklı-nadir atama** (~%35 taşıyıcı; **sabit 4-çekiliş bloğu** → paylaşılan-stream havuzlar seed-stabil kaldı). `EffectiveStrengthBuilder` ctx+trait+aura okur; `LeagueSeason` haftalık güç türetmesine `MatchContext`'i geçirir → **aynı 11, bahis farklı → skor farklı** (uçtan uca testle kilitli — Faz 4 çıkış kriterinin trait yarısı). Gelişim trait'leri karar #21/#23'ün borcunu kapatır (id-proxy değil gerçek sürücü); cam-adamın zirve-kayması **floor'dan sonra** uygulanır (floor yutarsa trait flavor olurdu). **(b) Dram motoru (TDD §8):** saf `Domain/Drama` şeması — `DramaEvent` (kategori + `DramaTrigger` koşulları [yaş/rating/potansiyel-gap/yedek/serî/pozisyon/pencere/**gerekli-trait**] + ağırlık + cooldown + once-per-run + **≥2 `DramaChoice` → `DramaEffect[]`**) + `DramaCatalog.Default` (**10 olay**: transfer-talebi [reddet/sat/ikna], gece-kulübü-skandalı, soyunma-odası-çatlağı, taraftar-protestosu, dakika-isteyen-wonderkid, bütçe-kesintisi, **kaptan-halefiyet** [emekli lider aurayı varise devreder — GrantTraitToSuccessor: 24-altı en iyisi, determinist], kulüp-satışı [set-piece, once-per-run], basın-savaşı [yalnız basın-mıknatısı öznesi], kontrat-restleşmesi). `DramaEngine` haftalık tick: durum-anlık-görüntüsünü filtrele → taban × özne/kadro trait-bias ağırlığı → kıtlık zarfı (**sezon bütçesi 4 + min-ara 4 hafta + cooldown**) → **ateşleme olasılığı toplam ağırlıkla ölçekli** (bias olayın FREKANSINI değiştirir, sadece seçim sırasını değil — lider odadayken çatlak ölçülebilir yarıya iner) → enjekte rng ile seç; `Resolve(choice)` moral etkilerini ledger'a uygular, **nakit/satış/trait-devri sahiplerine outcome olarak döner** (command in → outcome out). **(c) Moral = dramın sahaya eli:** `MoraleLedger` (oyuncu-başına, haftalık-sönümlü, kenetli istifler) + `IPlayerConditionSource` portu → `EffectiveStrengthBuilder` her rating'i çarpar; `LeagueSeason` ledger'ı taşır ve haftada yaşlandırır → **çözülen olay sonraki haftaların skorunu ölçülebilir kaydırır ve programında söner** (uçtan uca testle kilitli — çıkış kriterinin dram yarısı). **(d) Authoring (Unity-doğrulaması bekliyor):** `TraitSO`/`TraitCatalogSO`/`DramaEventSO`/`DramaCatalogSO` (config-as-override → boş/yok = Default) + `DramaBalanceSO` (frekans zarfı; `BalanceAssets`'e eklendi); katalog **enjeksiyonu çekirdeğe işlendi** (`LeagueSeason`/`SeasonTransition`/`SeasonSaveMapper.Restore`/`PlayerGenerator` opsiyonel `TraitCatalog`). `ManagementWindow`: kadro+market satırlarında **trait rozetleri**, haftalık dram tick'i (kendi seed-stream'i), olay çıkınca **karar kartı** (Advance'i bloklar; Play-to-End hikâyede durur), nakit/satış/halef-devri mevcut finans/transfer yollarından akar; setup'ta 3 içerik/balans SO alanı. **Ertelendi:** moral + dram-motoru-durumunun save kalıcılığı (taktik/ekonomi ile aynı sınıf), `MatchImportance`'ın fikstürden türetilmesi (şu an editör ctx'i Normal — derbi/final bahisleri sim+testte gerçek, lig loop'unda işaretleme Faz 5 anlatı işiyle doğal gelir), dram → JourneyLog/anlatı köprüsü (Faz 5). Ölçüm: 210 test yeşil (+38); frekans regresyonu 20 sezonda sezon-başına ~0.75–4 bandında.

32. **Zero-alloc pass + soru-bankası-v2 doküman ekleri + Tier-2 balans→SO göçü (standartlara tam uyum).** Üç iş, `feature/zero-alloc-and-balance-so`. **(a) Zero-alloc pass (PERFORMANCE §8 house rules, 2026-07-23 denetiminin bulguları):** sıcak yollardaki (maç/hafta/sezon-başına) tüm `IReadOnlyList<T>` `foreach`'leri **index `for`'a** çevrildi (boxed enumerator kalktı — `EffectiveStrengthBuilder`/`DramaEngine`/`MatchSimulator`/`LineupSelector`/`PlayerDevelopment`/`SeasonTransition`/`SquadRenewal`/`MoraleLedger`); `Formation` preset'leri `=> new` property'den **`static readonly` alana** (F442 fallback'i her maçta 11'lik dizi alloc ediyordu); scratch listeler **reuse edilen member buffer** oldu (`LineupSelector._available`, `DramaEngine._candidateScratch`, `PoissonChanceGenerator._chances` [dönen liste bir-sonraki-çağrıya-dek geçerli — simülatör senkron tüketiyor], `MoraleLedger._emptiedScratch`); `SplitMix64RandomNumberGenerator.Reseed(seed)` eklendi → maç-başına (`LeagueSeason`) ve oyuncu-başına (`SeasonTransition`/`SquadRenewal`) RNG'ler **tek instance'ı reseed'ler** (reseed = new ile birebir aynı stream → determinizm bit-aynı); `MatchSimulator` Sort'u **cached `IComparer` singleton**. 210 test yeşil, sim istatistikleri birebir aynı. **(b) Doküman ekleri (bankanın v2'sine karşı ikinci gap taraması):** CONVENTIONS §6 += Turkish-i/culture-invariant string kuralı + foreach-içinde-koleksiyon-mutasyonu + NaN/Infinity sessiz-zehirlenmesi; UNITY.md += **§6 async/threading doğruluğu** (continuation GameObject'ten uzun yaşar, main-thread-only API, `ConfigureAwait` yasağı, `async void` kuralı) + **§7 mobil kalıcılık tuzakları** (persistentDataPath, atomik yazma+bozuk-save fallback, versiyonlu şema, JsonUtility sınırları, PlayerPrefs kapsamı, BinaryFormatter yasağı, SO≠state, Android StreamingAssets); PERFORMANCE §7 += tween-başına yaşam süresi (`SetLink`, update-içinde-tween tuzağı, `OnComplete` closure, `SetUpdate(true)`), §12 += IL2CPP AOT kısıtları. **(c) Tier-2 balans→SO göçü ("BalanceSO later" yorumlarının tamamı kapandı):** dört yeni saf settings — `TacticsSettings` (mentality/pressing güç adımları + tempo/approach hacim-kalite çarpanları; `EffectiveStrengthBuilder` + `ChanceProfile.FromTactics` tüketir), `ScorerWeights` (golcü-atama pathway ağırlıkları + pozisyon payları; `WeightedScorerSelector` tüketir), `MoraleSettings` (puan-başına form + kenet; `MoraleLedger` tüketir), `EconomySettings` (değer/maaş tavan+yuvarlama+yaş eğrisi; `PlayerValuation`/`PlayerWage`/`TransferService`/`DramaEngine`-maaş-cezası tüketir) — hepsi config-as-override (null/parametresiz = kalibre Default, mevcut çağrılar bozulmadı, davranış bit-aynı). Authoring: `SimulationBalanceSO` genişledi (+chance-quality cap/varyans + taktik + golcü blokları; `ToTacticsSettings`/`ToScorerWeights`), `DramaBalanceSO` += moral bloğu (`ToMoraleSettings`), **yeni `EconomyBalanceSO`** (`BalanceAssets.Economy()` + menü). `LeagueSeason`/`SeasonSaveMapper.Restore` tactics+morale settings'i taşır; `ManagementWindow` tüm ayarları SO'lardan geçirir (+Economy ObjectField), `SeasonPlayerWindow` tactics+scorer'ı geçirir. **Karar — `PlayerRatings` rol-ağırlık tabloları bilinçle kodda kaldı:** bunlar balans dial'ı değil **rol modeli** (rolü hangi attribute'ların oluşturduğu; toplam-1 normalize) — ayarlamak her rating/değer/maaşı birden yeniden şekillendirir, yani tweak değil tasarım değişikliği; balans ayarı yukarıdaki settings nesnelerinden yapılır (gerekçe kod yorumunda). *(Editör + Infrastructure dosyaları Unity'de derle+gözle doğrula; yeni 5 .cs'nin .meta'ları Unity ilk açılışta üretilecek. Saf çekirdek: 210 test yeşil, istatistikler değişmedi.)*

---

## Kalan / sıradaki

- **Faz 4 playtest geri bildirimi (2026-07-18, kullanıcı) — SIRADAKİ DİLİMİN İŞİ:**
  1. **Dram kartları yapay hissettiriyor.** Kart şu an humanize edilmiş slug + jenerik bir dev-cümlesi gösteriyor (localization key'lerin arkasında henüz kopya yok). Çözüm yönü: olaylara özne/bağlam-farkında gerçek kopya (EN referans; string table Faz 7), gövde metninde skor-durumu/hafta/isim geçmesi; "yazılmış gibi" hissin asıl kaynağı Faz 5 anlatı+hafıza katmanı — dram kartını JourneyLog beat'lerine bağlamak oraya.
  2. **Gece kulübü skandalı çok sık.** Teşhis: özne-gerektiren olaylarda **her uygun oyuncu ayrı aday** → olayın etkin ağırlığı uygun-oyuncu-sayısıyla çarpılıyor (skandal `MaxSubjectAge 30` → kadroda ~15 uygun × 0.8 ≈ 12 ağırlık; transfer-talebi/wonderkid gibi dar filtreli olaylar 0–2 aday) ve ateşleme olasılığı toplam ağırlıkla ölçekli → geniş-filtreli olay hem ateşlemeyi hem seçimi domine ediyor. Çözüm yönü (çekirdek): olay-başına adaylığı **özne-sayısına normalize et** — olay tek aday olsun (ağırlık = taban × ortalama/maks özne-bias'ı), özne ikinci bir çekilişle seçilsin; ardından **frekans-karışımı regresyon testi** (uzun simde olay-başına pay bandı — hiçbir olay toplamın ~%40'ından fazlasını alamaz). Ağırlıklar artık asset'ten de ayarlanabilir ama yapısal düzeltme çekirdeğe ait.
  3. **Seçimin +'sı/-'si anlaşılmıyor.** Karar kartı seçenek düğmelerinde **etki önizlemesi** göstermeli (moral ±N kim/kaç hafta, nakit ±, satış, trait-devri — `DramaEffect` verisinden türetilebilir, ek veri gerekmez); çözüm sonrası "ne oldu" satırı var ama etkinin sonraki haftalara dokunduğu **görünmüyor** → roster'da aktif moral göstergesi (± rozet/renk) eklenmeli. Shipped UI'de bu kopya localization'dan gelecek; editör kartı dev-metniyle şimdi gösterebilir.
- ✅ **İçerik SO'ları üretilip editöre atandı (bu geri bildirimle birlikte):** `ContentAssets` (Editor/Content) — ilk kullanımda `TraitCatalog.Default`/`DramaCatalog.Default`'tan **trait-başına + olay-başına asset** üretir (`Assets/_Project/Settings/Content/{Traits,Drama}/`, Unity API'siyle — elle YAML yok, karar #28 kuralı), katalog asset'lerine bağlar; **var olan asset'e asla dokunmaz** → Inspector'da yapılan ayar kalıcı. `ManagementWindow` açılışta balance SO'ları gibi katalogları da otomatik atar; `Gaffer/Content/Create Default Content Assets` menüsü de var. SO'lara `Author(saf-tanım)` metodları eklendi (tersine map). *(Unity'de derle + asset'lerin oluştuğunu gözle doğrula.)*

- **Faz 3 (Yönetim, devam) — sıradaki:** ✅ `Squad` + `BuildEffectiveStrength` + `SquadGenerator` + `Club`↔`Squad` + gruplu attribute modeli + isimli golcü seçimi + **taktik** (`Tactics`) + **ilk 11 seçimi** (`Formation` + `LineupSelector`; güç sahadaki 11'den, Season Player'da formasyon + Start/Bench) hazır. ✅ **scout maskesi + değerleme + al/sat ekonomisi + maaşlar** (`Scout`/`PlayerValuation`/`PlayerWage`/`Finances`/`TransferService`, CM-sadık iki-bütçe; Transfer Market penceresi nakit+maaş+Sign/Sell ile gösteriyor) hazır → keşfet-**sat** döngüsü kapandı. ✅ **granüler mevki** (`PlayerRole`/`PlayerRoles` + rol-slotlu `Formation` + rol-dolduran `LineupSelector` + rol-dağıtan `SquadGenerator`; pencereler spesifik rolü gösteriyor) hazır. ✅ **rol-özel rating** (`PlayerRatings.ForRole`: bek pace/crossing, stoper heading — aynı stat mevkiye göre farklı değerde; karar #20) hazır. ✅ **oyuncu gelişimi** (`PlayerDevelopment`: genç cevher potansiyele büyür, veteran rol+oyuncu-özel zirveden sonra düşer; karar #21) hazır. ✅ **gelişimin sezon-entegrasyonu** (`SeasonTransition`; karar #22) + ✅ **emeklilik + genç-alımı** (`SquadRenewal`: yaşlılar emekli, aynı mevkide genç gelir, kadro boyu+seviye kalıcı; Season Player "SUMMER" kartı; karar #23) + ✅ **garanti-cevher intake-tohumlaması** (kulüp-başına kadans, seyrek+gizli, yüzde değil; keşif gelişerek belirir; karar #24) hazır → **sürdürülebilir çok-sezonlu run + süregelen keşif**, keşfet-**büyüt**-sat flip'i gerçek lig loop'unda kapandı. ✅ **çok-sezon save kalıcılığı** (`SeasonSaveData` v3: kadrolar + sezon-no round-trip; karar #25) + ✅ **JSON save adapter** (`NewtonsoftJsonSerializer`+`JsonSaveStore`, Season Player Save/Load; JSON round-trip headless kilitli — karar #26; **Unity'de derle+gözle-doğrula kaldı**) + ✅ **kulüp/lig üreteci** (`ClubNameGenerator`+`LeagueGenerator`; Season Player + harness üretilmiş isim kullanır; karar #27) hazır → **dünya artık uçtan uca üretilir**. ✅ **BalanceSO tamamlandı** (sim + gelişim + renewal üçü de veri-güdümlü: 3 SO + saf settings; **Season Player balance workbench**'inde editörde uçtan uca test edilebilir; karar #28) hazır. **Sıradaki:** ✅ **Faz 4 çekirdeği** (trait sistemi + dram motoru + moral; karar #31 — SO yüzeyleri + editör dram kartı **Unity'de derle+gözle doğrula** kaldı) → **Faz 5 — Hafıza + Anlatı** (JourneyLog + maç anlatısı + satış anı + recap; ★ Gate B). *(Save'de taktik/dizilim/ekonomi/moral kalıcılığı + `MatchImportance`'ın fikstürden türetilmesi [derbi işaretleme] + async I/O + arma üretimi sonraya.)*
- **Gelişim — sonraki derinleşmeler (bilinçle ertelendi; çekirdek CM-eğrisi yeterli):** (1) **sezon-entegrasyonu** — LeagueSeason sezon-sonu tüm kadroları `Develop` ile yaşlandır+geliştir + save kalıcılığı [en yakın dilim]; (2) **oyuncuya-özel gerçek sürücü** — şimdiki id-proxy zirve-offset'inin yerine **profesyonellik/kişilik + trait** (`DevelopmentModifier`: "antrenman kaçkını"→gelişim penaltısı, "cam adam"→erken düşüş) [Faz 4]; (3) **oynama-süresi/form** gelişimi ve satılabilirliği besler (GDD §4.4) — dakika takibi gerektirir [sonraya]; (4) **basit antrenman** — haftalık odak (genç geliştir/form/fitness), MVP zorunluğu değil (GDD §4.5) [opsiyonel]; (5) gelişim sabitleri (büyüme oranları, zirve yaşları, varyans) `BalanceSO`'ya taşınır [sonraya].
- ✅ **Birleşik yönetim penceresi + sezon-içi transfer + ekonominin sezon-entegrasyonu (`ManagementWindow`; karar #29).** Transfer Market + Season Player **tek pencerede** birleşti → bir sezonu oynarken transfer yap + simüle et birlikte. **Sadece UI değil, ekonomi loop'ta:** (a) `Finances.PayWeeklyWages` maaşları haftalık nakitten düşer (CAP→gerçek gider), (b) `LeagueSeason.UpdateSquad`/`SquadOf` imza/satışı aynı hafta kadroya/güce/ilk-11'e yansıtır, (c) `TransferWindow` Summer/Winter dönemleri. 169 yeşil (+12). **Unity'de derle+gözle-doğrula kaldı** (editör pencere kodu). **Ertelendi:** ekonomi/dizilim save-kalıcılığı, gelir modeli (gate/prize), AI kulüpler-arası transferler.
- **Faz 2 kapanışı:** `Infrastructure/Persistence` JSON impl **yapıldı** (karar #26; Unity'de derle+doğrula kaldı).
- **Bonus:** Editor harness'ı `Application/Season`'ı kullanacak şekilde sadeleştir (duplication).

---

## Nasıl koşulur (hatırlatma)

- **Testler:** `PATH="$HOME/.dotnet:$PATH" dotnet test tests/Gaffer.Tests.csproj`
- **Harness:** Unity → menü **`Gaffer > Season Harness`** → Run
- **Management (birleşik demo):** Unity → menü **`Gaffer > Management`** → Start Season → Advance Week (nakit erir) + Summer/Winter'da Sign/Sell (canlı kadro)
- **Season Player (demo):** Unity → menü **`Gaffer > Season Player`** → Start Season → Advance Week
- **Transfer Market (demo):** Unity → menü **`Gaffer > Transfer Market`** → Generate Market → Scout accuracy slider'ı (aralıklar daralır) + Sign/Sell (nakit + maaş bütçesi)
- **Maç smoke:** Unity → `Assets/_Project/Scenes/MatchSmokeTest.unity` → **Play** (Console'a maç akar)

---

*Her faz/dilim bittikçe bu doküman güncellenir. Kararlar değişirse eski karar üstü çizilmeden güncellenir, gerekçe not düşülür.*
