namespace Gaffer.Presentation.Shell
{
    /// <summary>The shell's tabs, in the order they sit on the bar. The fourth — the manager — arrives
    /// with Faz 6 and takes its place at the end.</summary>
    public enum ShellTab
    {
        Squad = 0,
        Season = 1,
        Market = 2,
    }
}
