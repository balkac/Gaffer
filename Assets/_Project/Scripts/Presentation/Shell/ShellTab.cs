namespace Gaffer.Presentation.Shell
{
    /// <summary>The shell's tabs, in the order they sit on the bar. The fourth is the menu — Save, Main
    /// menu, and from Faz 6 the manager's own page (reputation, perks, the Hall of Fame) — so the tab a
    /// manager learns today is the tab the meta arrives on; nothing moves later.</summary>
    public enum ShellTab
    {
        Squad = 0,
        Season = 1,
        Market = 2,
        Menu = 3,
    }
}
